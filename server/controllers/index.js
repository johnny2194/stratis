const users = require('./users')
const products = require('./products')
const stock_details = require('./stock_details')
const orders = require('./orders')
const purchases = require('./purchases')
const auth = require('./auth')

module.exports = {
  users,
  products,
  stock_details,
  orders,
  purchases,
  auth
};